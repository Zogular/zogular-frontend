/**
 * Temporary MVP frontend config. 
 * Backend should own this once payment/order accounting is implemented.
 */
export const COD_COMMITMENT_FEE_RATE = 0.1;
export const PAYMENT_COLLECTION_MODE = "commitment_fee_then_cash";
